apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
spec:
  rules:
  - host:
  http:
    path:
    - pathType: Prefix
     path: /haykel
     backend:
       service:
         name: servicenew
         port:
           number:3333
    
